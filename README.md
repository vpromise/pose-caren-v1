# pose-caren.pytorch
human pose estimation in pytorch on CAREN datasets

## Datasets
CAREN Dataset

## Requirements
pytorch
torchvision
visdom

## Traing
`python -m visdom.server` 打开 `Visdom` 服务器
`python train.py` 即可

## Test
`python test.py`
